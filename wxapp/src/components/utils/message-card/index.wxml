<van-cell center clickable value="{{date}}" label="{{summary}}" bind:click="onTap">
  <view slot="title" class="title-box">
    <view class="title">{{title}}</view>
    <van-tag wx:if="{{unread}}" type="danger">未读</van-tag>
  </view>
</van-cell>
